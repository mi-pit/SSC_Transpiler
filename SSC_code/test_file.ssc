superstruct Adder {
    unsigned long int x;
    int *y;

    int a, b, c;

    int add(signed int x, unsigned long long int *hopspop) {
        return this->x + x;
    }

    signed sub()
    {
        signed out = x - y;
        return out--;
    }
};

int main( void )
{
    // hopspop
    // superstruct Adder a;

    return 0;
}
